# AI-Accelerator

This repository contains a simple web tool for planning AI bootcamps. Administrators can log in and build interactive agendas by creating days and learning items. Items can be dragged between days and expanded to show links or descriptions.

Open `src/index.html` in a browser to try it locally. The default admin credentials are:

```
username: admin
password: password
```

